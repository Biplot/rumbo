# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Dependencias (por si más adelante)
node_modules/

# Teaser: directorio de trabajo del render (fotogramas, música, videos en curso).
# Los entregables finales se versionan en teaser/video/.
teaser/out/
